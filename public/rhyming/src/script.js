/*
  TODO
    get phonemes
    split into syllables (consonants/vowels)
    display syllables, with weights on each syllable
    rate and sort closest words (with syllable offset)
    display possible "alternatives"
*/

const { send } = setupConnection("wordplay", (message) => {
  // FILL
});
